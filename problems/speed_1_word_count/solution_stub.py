"""
SPEED WARMUP 1 — Word Count (target: under 5 min)

Implement word_count(text) that returns a dict mapping each word to its
count. Words are separated by whitespace. Case-insensitive.

Example:
    word_count("The quick brown fox the FOX")
    -> {"the": 2, "quick": 1, "brown": 1, "fox": 2}

Then implement top_words(text, n) that returns the n most common words
as a list of (word, count) tuples, sorted by count descending, then by
word ascending for ties.

Run: python -m unittest test_word_count.py -v
"""


def word_count(text):
    raise NotImplementedError


def top_words(text, n):
    raise NotImplementedError
